const errorHandler = require("./errorHandler");
const errorHandlerWrapper = require("./errorHandlerWrapper");
const errorPageGenerator = require("./errorPageGenerator");
const errors = require("./errors");

module.exports = {
  errorHandler,
  errorHandlerWrapper,
  errorPageGenerator,
  errors,
};
